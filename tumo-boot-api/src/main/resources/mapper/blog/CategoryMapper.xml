<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tycoding.boot.modules.blog.mapper.CategoryMapper">

    <select id="articleList" resultType="cn.tycoding.boot.modules.blog.entity.Category">
        SELECT `article`.id,
               `article`.title,
               `article`.author,
               `article`.create_time
        FROM blog_article_category bac,
             blog_article `article`
        WHERE bac.article_id = `article`.id
          AND bac.category_id = #{id}
    </select>
</mapper>
