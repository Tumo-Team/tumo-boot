version: "3.5"

services:
  tumo-boot-api:
    image: "172.16.60.134/tumo-boot/tumo-boot-api"
    hostname: "tumo-boot-api"
    environment:
      - TZ=Asia/Shanghai
    ports:
      - 8087:8080
    restart: always
